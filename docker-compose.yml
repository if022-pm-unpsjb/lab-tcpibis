services:

  infracciones_principal:
    image: elixir:1.15.7-alpine
    container_name: infracciones_principal
    network_mode: "host"
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      MIX_HOME: /app/mix_home
      HEX_HOME: /app/hex_home
      AMQP_URL: "amqps://euurcdqx:pXErClaP-kSXdF8YZypEyZb5brqWRthx@jackal.rmq.cloudamqp.com/euurcdqx"
      SERVER_TO_RUN: Elixir.Libremarket.Infracciones.Server
      AMQP_TO_RUN: Elixir.Libremarket.Infracciones.AMQP
      PRIMARY: true
      CONTAINER_NAME: infracciones_principal
    command: 
      elixir --sname infracciones_principal --cookie ${SECRET} -S mix run --no-halt --no-compile    # para habilitar -it
    # para habilitar -it
    stdin_open: true
    tty: true
    user: "${DOCKER_UID}:${DOCKER_GID}"
  
  infracciones_replica_1:
    image: elixir:1.15.7-alpine
    container_name: infracciones_replica_1
    network_mode: "host"
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      MIX_HOME: /app/mix_home
      HEX_HOME: /app/hex_home
      AMQP_URL: "amqps://euurcdqx:pXErClaP-kSXdF8YZypEyZb5brqWRthx@jackal.rmq.cloudamqp.com/euurcdqx"
      SERVER_TO_RUN: Elixir.Libremarket.Infracciones.Server
      AMQP_TO_RUN: Elixir.Libremarket.Infracciones.AMQP
      PRIMARY: false
      CONTAINER_NAME: infracciones_replica_1
    command: 
      elixir --sname infracciones_replica_1 --cookie ${SECRET} -S mix run --no-halt --no-compile    # para habilitar -it
    # para habilitar -it
    stdin_open: true
    tty: true
    user: "${DOCKER_UID}:${DOCKER_GID}"
 
  infracciones_replica_2:
    image: elixir:1.15.7-alpine
    container_name: infracciones_replica_2
    network_mode: "host"
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      MIX_HOME: /app/mix_home
      HEX_HOME: /app/hex_home
      AMQP_URL: "amqps://euurcdqx:pXErClaP-kSXdF8YZypEyZb5brqWRthx@jackal.rmq.cloudamqp.com/euurcdqx"
      SERVER_TO_RUN: Elixir.Libremarket.Infracciones.Server
      AMQP_TO_RUN: Elixir.Libremarket.Infracciones.AMQP
      PRIMARY: false
      CONTAINER_NAME: infracciones_replica_2
    command: 
      elixir --sname infracciones_replica_2 --cookie ${SECRET} -S mix run --no-halt --no-compile    # para habilitar -it
    # para habilitar -it
    stdin_open: true
    tty: true
    user: "${DOCKER_UID}:${DOCKER_GID}"

  envios:
    image: elixir:1.15.7-alpine
    container_name: envios
    network_mode: "host"
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      MIX_HOME: /app/mix_home
      HEX_HOME: /app/hex_home
      AMQP_URL: "amqps://euurcdqx:pXErClaP-kSXdF8YZypEyZb5brqWRthx@jackal.rmq.cloudamqp.com/euurcdqx"
      SERVER_TO_RUN: Elixir.Libremarket.Envio.Server
      AMQP_TO_RUN: Elixir.Libremarket.Envio.AMQP
      PRIMARY: true
    command: 
      elixir --sname envios --cookie ${SECRET} -S mix run --no-halt --no-compile    # para habilitar -it
    # para habilitar -it
    stdin_open: true
    tty: true
    user: "${DOCKER_UID}:${DOCKER_GID}"

  compras_principal:
    image: elixir:1.15.7-alpine
    container_name: compras_principal
    network_mode: "host"
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      MIX_HOME: /app/mix_home
      HEX_HOME: /app/hex_home
      AMQP_URL: "amqps://euurcdqx:pXErClaP-kSXdF8YZypEyZb5brqWRthx@jackal.rmq.cloudamqp.com/euurcdqx"
      SERVER_TO_RUN: Elixir.Libremarket.Compras.Server
      AMQP_TO_RUN: Elixir.Libremarket.Compras.AMQP
      PRIMARY: true
      CONTAINER_NAME: compras_principal
    command: 
      elixir --sname compras_principal --cookie ${SECRET} -S mix run --no-halt --no-compile    # para habilitar -it
    # para habilitar -it
    stdin_open: true
    tty: true
    user: "${DOCKER_UID}:${DOCKER_GID}"

  compras_replica_1:
    image: elixir:1.15.7-alpine
    container_name: compras_replica_1
    network_mode: "host"
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      MIX_HOME: /app/mix_home
      HEX_HOME: /app/hex_home
      AMQP_URL: "amqps://euurcdqx:pXErClaP-kSXdF8YZypEyZb5brqWRthx@jackal.rmq.cloudamqp.com/euurcdqx"
      SERVER_TO_RUN: Elixir.Libremarket.Compras.Server
      AMQP_TO_RUN: Elixir.Libremarket.Compras.AMQP
      PRIMARY: false
      CONTAINER_NAME: compras_replica_1
    command: 
      elixir --sname compras_replica_1 --cookie ${SECRET} -S mix run --no-halt --no-compile    # para habilitar -it
    # para habilitar -it
    stdin_open: true
    tty: true
    user: "${DOCKER_UID}:${DOCKER_GID}"

  compras_replica_2:
    image: elixir:1.15.7-alpine
    container_name: compras_replica_2
    network_mode: "host"
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      MIX_HOME: /app/mix_home
      HEX_HOME: /app/hex_home
      AMQP_URL: "amqps://euurcdqx:pXErClaP-kSXdF8YZypEyZb5brqWRthx@jackal.rmq.cloudamqp.com/euurcdqx"
      SERVER_TO_RUN: Elixir.Libremarket.Compras.Server
      AMQP_TO_RUN: Elixir.Libremarket.Compras.AMQP
      PRIMARY: false
      CONTAINER_NAME: compras_replica_2
    command: 
      elixir --sname compras_replica_2 --cookie ${SECRET} -S mix run --no-halt --no-compile    # para habilitar -it
    # para habilitar -it
    stdin_open: true
    tty: true
    user: "${DOCKER_UID}:${DOCKER_GID}"

  pagos_principal:
    image: elixir:1.15.7-alpine
    container_name: pagos_principal
    network_mode: "host"
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      MIX_HOME: /app/mix_home
      HEX_HOME: /app/hex_home
      AMQP_URL: "amqps://euurcdqx:pXErClaP-kSXdF8YZypEyZb5brqWRthx@jackal.rmq.cloudamqp.com/euurcdqx"
      SERVER_TO_RUN: Elixir.Libremarket.Pagos.Server
      AMQP_TO_RUN: Elixir.Libremarket.Pagos.AMQP
      PRIMARY: true
      CONTAINER_NAME: pagos_principal
    command: 
      elixir --sname pagos_principal --cookie ${SECRET} -S mix run --no-halt --no-compile    # para habilitar -it
    # para habilitar -it
    stdin_open: true
    tty: true
    user: "${DOCKER_UID}:${DOCKER_GID}"

  pagos_replica_1:
    image: elixir:1.15.7-alpine
    container_name: pagos_replica_1
    network_mode: "host"
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      MIX_HOME: /app/mix_home
      HEX_HOME: /app/hex_home
      AMQP_URL: "amqps://euurcdqx:pXErClaP-kSXdF8YZypEyZb5brqWRthx@jackal.rmq.cloudamqp.com/euurcdqx"
      SERVER_TO_RUN: Elixir.Libremarket.Pagos.Server
      AMQP_TO_RUN: Elixir.Libremarket.Pagos.AMQP
      PRIMARY: false
      CONTAINER_NAME: pagos_replica_1
    command: 
      elixir --sname pagos_replica_1 --cookie ${SECRET} -S mix run --no-halt --no-compile    # para habilitar -it
    # para habilitar -it
    stdin_open: true
    tty: true
    user: "${DOCKER_UID}:${DOCKER_GID}"

  pagos_replica_2:
    image: elixir:1.15.7-alpine
    container_name: pagos_replica_2
    network_mode: "host"
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      MIX_HOME: /app/mix_home
      HEX_HOME: /app/hex_home
      AMQP_URL: "amqps://euurcdqx:pXErClaP-kSXdF8YZypEyZb5brqWRthx@jackal.rmq.cloudamqp.com/euurcdqx"
      SERVER_TO_RUN: Elixir.Libremarket.Pagos.Server
      AMQP_TO_RUN: Elixir.Libremarket.Pagos.AMQP
      PRIMARY: false
      CONTAINER_NAME: pagos_replica_2
    command: 
      elixir --sname pagos_replica_2 --cookie ${SECRET} -S mix run --no-halt --no-compile    # para habilitar -it
    # para habilitar -it
    stdin_open: true
    tty: true
    user: "${DOCKER_UID}:${DOCKER_GID}"

  ventas:
    image: elixir:1.15.7-alpine
    container_name: ventas
    network_mode: "host"
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      MIX_HOME: /app/mix_home
      HEX_HOME: /app/hex_home
      AMQP_URL: "amqps://euurcdqx:pXErClaP-kSXdF8YZypEyZb5brqWRthx@jackal.rmq.cloudamqp.com/euurcdqx"
      SERVER_TO_RUN: Elixir.Libremarket.Ventas.Server
      AMQP_TO_RUN: Elixir.Libremarket.Ventas.AMQP
      PRIMARY: true
    command: 
      elixir --sname ventas --cookie ${SECRET} -S mix run --no-halt --no-compile    # para habilitar -it
    # para habilitar -it
    stdin_open: true
    tty: true
    user: "${DOCKER_UID}:${DOCKER_GID}"
 
  rest:
    image: elixir:1.15.7-alpine
    container_name: rest
    network_mode: "host"              # igual que los demás
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      MIX_HOME: /app/mix_home
      HEX_HOME: /app/hex_home
      SERVER_TO_RUN: Elixir.Libremarket.Rest.Server   # 👈 arranca el server HTTP
      REST_PORT: 4000                                  # 👈 puerto del Plug/Cowboy
      SECRET: ${SECRET}
    command: >
      elixir --sname rest --cookie ${SECRET}
      -S mix run --no-halt --no-compile
    user: "${DOCKER_UID}:${DOCKER_GID}"
